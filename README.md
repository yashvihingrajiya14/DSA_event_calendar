# DSA_event_calendar
A college event scheduler built with HTML, CSS &amp; Vanilla JS, applying DSA concepts — Array, Insertion Sort, Stack (undo), and Queue (conflict handling). Conflicting events auto-queue and get scheduled when a slot frees up. Includes dark mode and live stats.
